/* These configuration features are to conditionally render functionality that is not yet resolved
from the backend. */
export const featuresConfig = {
  showReformulate: false,
  showOrderChats: false,
  showProfileSettings: false,
  showMessageTime: false,
};
